<?php

namespace DashboardUserDmer;

defined('ABSPATH') || die();

class DashboardUserDmer
{
    public function init()
    {
        // create table for msgs
            // table = id | msg | for who is this msg (also based on user role) | who wrote the msg | time | viewed (when was)

        // one menu link for a msgs page

        // msgs icon on admin bar

        // admin ui for writting msgs -- use heartbeat api and JS for constant update

        // css enqueue

        // js enqueue

        // ajax
    
    }
}
